#include <stdio.h>
void main()
{
    int m;
    scanf("%d",&m);
    if (m==0)
        {printf("Zero");}
    else if (m<0)
        {printf("Negative");}
    else
        {printf("Positive");}
}