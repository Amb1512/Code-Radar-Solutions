#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    char c = 64 + j;
    for (int i =  1; i <= n; i++)
    {
        for (int j = 1; j <= n-i; j++)
        {
            printf("%c ", c);
        }
    }
}